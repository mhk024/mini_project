# Lean production deps for Render 512MB (no torch / sentence-transformers by default)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-ollama>=0.1.0
langchain-text-splitters>=0.2.0
chromadb>=0.4.24
fastembed>=0.3.0
pypdf>=4.0.0
httpx>=0.27.0
pydantic>=2.0.0
numpy>=1.26.0,<2.0.0

# Frontend (deploy as separate Render service or run locally)
streamlit>=1.32.0
requests>=2.31.0
plotly>=5.18.0

# Optional — local dev / cross-encoder reranking (adds ~400MB+ RAM)
# langchain-huggingface>=0.0.3
# sentence-transformers>=2.6.0
# torch --index-url https://download.pytorch.org/whl/cpu
